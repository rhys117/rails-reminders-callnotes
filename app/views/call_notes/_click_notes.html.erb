<div id='enquiry-templates'>
  <ul class="nav nav-tabs justify-content-center">
    <% groups.each do |group| %>
      <li class="nav-item <%= 'active' if active_class?(params[:cat_id], group) %>">
        <%= link_to group, fetch_enquiry_templates_path(:cat_id => group), :remote => true, form_class: 'nav-link' %>
      </li>
    <% end %>
  </ul>

  <div class='form-group'>
    <% items.each do |key, value| %>
      <%= link_to key, fetch_selected_template_path(:template => key, :type => type, :cat_id => "#{params[:cat_id]}"), :remote => true, class: 'btn btn-default btn-sm btn-margin-top', onclick: "PrependToEnquiryNotes('#{key.parameterize.underscore}')" %>
      <textarea class='copy-data' id="<%= key.parameterize.underscore %>"><%= value %></textarea>
    <% end %>
  </div>
</div>
